// Creation of a function in pl/sql

create or replace function fact(x number)
	return number
is
	f number;
begin
	if x = 0 then
		f := 1;
	else
		f := x*fact(x-1);
	end if;
	return f;
end;


// How to use

declare
	res number;
begin
	res := fact(5);
	dbms_output.put_line(res);
end;